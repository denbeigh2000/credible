use std::io;
use std::path::Path;

pub fn mount_ramfs(_dir: &Path) -> Result<(), std::io::Error> {
    // use nix::mount::MsFlags;

    // let mut flags = MsFlags::empty();
    // flags.add(MsFlags::
    unimplemented!();
    // nix::mount::mount(None, &self.secret_root, "ramfs",
}
